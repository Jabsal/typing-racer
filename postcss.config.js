/**
 * PostCSS configuration for the typing racer project.  PostCSS is
 * used to process your Tailwind CSS and add vendor prefixes when
 * necessary.  The exported configuration object defines which
 * plugins are active; here we enable Tailwind and autoprefixer.
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};